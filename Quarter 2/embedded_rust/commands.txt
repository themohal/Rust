//for building

cargo build --target thumbv7em-none-eabihf


//for reading header

cargo readobj --target thumbv7em-none-eabihf --bin led-roulette -- -file-headers

//for opening ocd channel change directory

cd /tmp

//run to establish connection to stm

openocd -f interface/stlink-v2-1.cfg -f target/stm32f3x.cfg

//to open gdb debugger shell

gdb-multiarch -q target/thumbv7em-none-eabihf/debug/led-roulette

//eastablish connection openocd and gdb write in gdb shell

target remote :3333

//flashing code in gdb shell write command

load

//for hello world program

create txt file in cd /tmp directory 

 # itmdump terminal

 # *nix
 cd /tmp && touch itm.txt

 # Windows
 cd %TEMP% && type nul >> itm.txt

 # both
 itmdump -F -f itm.txt //this command will halt the terminal


//to redirect input from mcu to host

(gdb) # globally enable the ITM and redirect all output to itm.txt
(gdb) monitor tpiu config internal itm.txt uart off 8000000

(gdb) # enable the ITM port 0
(gdb) monitor itm port 0 on

